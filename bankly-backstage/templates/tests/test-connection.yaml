apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bankly-backstage.fullname" . }}-test-connection"
  labels:
    {{- include "bankly-backstage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bankly-backstage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
